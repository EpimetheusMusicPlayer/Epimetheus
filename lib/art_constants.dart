import 'package:flutter/painting.dart';

const BorderRadius artBorderRadius = const BorderRadius.all(Radius.circular(4));
const Color artBackgroundColor = const Color(0xFF7E7E7E);
const int serviceArtSize = 1; //500;
